# Heavy ML/numerical deps cap the next major version (where breaking changes
# concentrate) so a plain-pip install won't silently pull an untested major.
# Kept in sync with pyproject.toml. (issue #4)
PyQt6>=6.6.0
sounddevice>=0.4.6
PyAudioWPatch>=0.2.12
numpy>=1.24.0,<3
faster-whisper>=1.0.0,<2
pyannote.audio>=4.0.0,<5
torch>=2.0.0,<3
torchaudio>=2.0.0,<3
scipy>=1.11.0,<2
pydub>=0.25.1
pywin32>=306
soundfile>=0.12.0
pycaw>=20230407
comtypes>=1.2.0
psutil>=5.9.0
transformers>=4.45.0,<6
sentence-transformers>=3.0.0,<6

# AI Providers — installed automatically when selected in Settings
# anthropic>=0.40.0          # Claude
# openai>=1.50.0             # OpenAI, Grok (xAI)
# google-generativeai>=0.8.0 # Google Gemini
# mistralai>=1.0.0           # Mistral
# groq>=0.11.0               # Groq (groq.com)
# llama-cpp-python>=0.3.0    # Local models
